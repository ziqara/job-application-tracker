# Job Application Tracker

**Пет-проект.** Небольшое фулстек-приложение, на котором я учился React,
TypeScript, NestJS и Prisma — от первых строк до рабочего продукта.

Трекер откликов на вакансии: список, добавление, удаление, смена статуса по
клику (Отправлено → Собеседование → Оффер → Отказ), фильтр по статусу.

## Стек

**Фронтенд**
- React 19 + TypeScript
- Vite
- Material UI

**Бэкенд**
- NestJS — модули, контроллеры, сервисы, Dependency Injection
- DTO + валидация (`class-validator`, `ValidationPipe`)
- Prisma + PostgreSQL

## Что показывает этот проект

- REST API с валидацией входных данных и корректными статус-кодами (200/201/204/400/404)
- Слой доступа к БД через ORM (Prisma), без сырого SQL в бизнес-логике
- Типизацию на обоих концах — от схемы БД до пропсов компонентов
- Контролируемые формы, производный (derived) state, обработку состояний загрузки/ошибки/пустого списка на фронте

## Структура репозитория

| Папка          | Что внутри                                                       |
| -------------- | ----------------------------------------------------------------- |
| `job-tracker/` | фронтенд на React + Vite                                          |
| `nest-server/` | REST API на NestJS, ходит в PostgreSQL через Prisma                |
| `ts-basics/`   | черновик — упражнения по TypeScript, с которых начинался проект   |

## API

| Метод  | Путь                | Что делает            |
| ------ | ------------------- | ---------------------- |
| GET    | `/applications`     | список всех откликов   |
| GET    | `/applications/:id` | одна заявка            |
| POST   | `/applications`     | создать отклик         |
| PATCH  | `/applications/:id` | обновить статус        |
| DELETE | `/applications/:id` | удалить отклик         |

## Запуск локально

Нужны Node.js 20+ и PostgreSQL.

### 1. Бэкенд

```bash
cd nest-server
npm install
cp .env.example .env   # вписать свои данные подключения к БД
npx prisma migrate dev # создаст базу и таблицу по схеме
npm run start:dev      # http://localhost:3000
```

### 2. Фронтенд

```bash
cd job-tracker
npm install
npm run dev             # http://localhost:5173
```

## Статус

Готово: фронт, бэкенд на NestJS + Prisma, полный CRUD, валидация, обработка
ошибок. Не сделано: деплой (сейчас проект рассчитан на локальный запуск).
